# this is a doc processing (CRUD) service that utilizes libreoffice 

## steps to run the service

1. navigate to the current directory 
2. run (open) the script '.\libreheadless.bat'
2. run the command '.\librepython document_service.py' OR run (open) the script '.\run_service.bat'
